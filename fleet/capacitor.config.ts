import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vfg.autoparts.front',
  appName: 'Fleet',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
